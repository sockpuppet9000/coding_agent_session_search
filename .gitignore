target
**/*.rs.bk
.DS_Store
.env
.idea/
.vscode/
*.swp
*.swo
/dist/
/node_modules/
tests/fixtures/search_demo_data/watch_state.json
/.venv/lib/python3.12/site-packages/googleapiclient/discovery_cache/documents
/.venv/lib/python3.12/site-packages/pygments/lexers
/.venv/lib/python3.12/site-packages/pygments/lexers/__pycache__

# AI coding tool artifacts
.aider.chat.history.md
.aider.input.history
.aider.tags.cache.v3/
!tests/fixtures/aider/.aider.chat.history.md

# Temporary scratch files
temp_*.rs

# Build artifacts
a.out

# Clippy / UBS / analysis output
clippy_output.txt
ubs.json
ubs_filtered.txt
ubs-report.*
*.sarif

# Agent-generated scratch scripts
parse_ubs.py
fix_*.py

# Local indexer working dirs and log output (regenerated on demand)
/data/
/logs/

# One-off test source files
test_empty_db.rs

# Search demo data is a checked-in robot CLI fixture; keep the frozen v1 index
# visible while ignoring later scratch generations produced by local runs.
!tests/fixtures/search_demo_data/watch_state.json
!tests/fixtures/search_demo_data/index/
tests/fixtures/search_demo_data/index/v*/
!tests/fixtures/search_demo_data/index/v1/
!tests/fixtures/search_demo_data/index/v1/**
tests/fixtures/search_demo_data/index/.lexical-publish-backups/
tests/fixtures/search_demo_data/index-run.lock
tests/fixtures/search_demo_data/doctor/
tests/fixtures/search_demo_data/tmp_home/

# bv (beads viewer) local config and caches
.bv/

# Claude Code artifacts
.claude/

# Local backup directories
.local_backup_*/

# Alternative target directories (benchmarks, tests, checks)
target_*/
target-*/
.target-*/

# Web test artifacts
web/node_modules/
web/playwright-report/
web/test-results/
web/test-fixtures/

# Performance test deps/artifacts
tests/performance/node_modules/

# Node.js test dependencies
tests/node_modules/

# Test output. Durable policy inputs belong under tests/policies/.
test-results/*
/test-results-*.txt
claude-upgrade-progress.json

# Root-level skill-loop/test scratch outputs. Durable plans belong under
# docs/planning/, durable evidence bundles under docs/artifacts/, and reference
# guides under docs/reference/.
/DASHBOARD.md
/final_dashboard.md
/baseline.md
/architecture.md
/pass*.md
/duplication_map.*
/skill_inventory.json
/slop_scan.md
/loc_delta_*.numstat
/baseline_*.log
/cargo_*.log
/touched_rustfmt_check.log
/validation_report.json
/per_query_scores.json

# Ephemeral E2E test artifacts
/artifacts/
tmp/
tests/e2e-report/
tests/e2e-results.json
tests/e2e/.env.test
tests/e2e/exports/
tests/e2e/pages_preview/
# Local generated test/profiling artifacts. Durable evidence belongs in docs/artifacts/.
tests/artifacts/
tests/test-results/
tests/tests/

# Python bytecode caches (global)
__pycache__/
*.pyc
.ruff_cache/

# Performance optimization artifacts
/perf/

# Claude subagent conversation exports
claude_subagents_*.html


# Browser screenshots (ephemeral debug artifacts)
Chrome_*.png
Google Chrome_*.png
*_screenshot*.png

# Useful project images belong under docs/assets/, not in the repo root.
/cass_illustration.webp
/gh_og_share_image.*
/screenshots/

# Dependency-upgrade notes belong under docs/planning/.
/UPGRADE_LOG.md

# Preserved planning docs must stay visible even if a developer-level global
# ignore file contains the old scratch filename from before docs/planning/.
!docs/planning/MODES_OF_REASONING_REPORT_AND_ANALYSIS_OF_PROJECT.md

# HTML export drafts
world_class_export.html
claude_*.html
session_export_test.html
tests/fixtures/search_demo_data/*.db-shm
tests/fixtures/search_demo_data/*.db-wal
session_export.html

# Local sqlite artifacts in repo root
/*.db
/*.db-wal
/*.db-shm
/*.sqlite
/*.sqlite-wal
/*.sqlite-shm
/storage.sqlite3
/storage.sqlite3-wal
/storage.sqlite3-shm
# SQLite WAL/SHM/journal files are process-local sidecar state. Durable fixture
# databases may be checked in deliberately, but their sidecars should not be.
*.db-wal
*.db-shm
*.db-journal
*.sqlite-wal
*.sqlite-shm
*.sqlite-journal
*.sqlite3-wal
*.sqlite3-shm
*.sqlite3-journal
*.sqlite3.bak
*.sqlite3.bak-*
*.sqlite3-wal.bak
*.sqlite3.corrupt-*
*.sqlite3-wal.corrupt-*

# Agent probe/scratch files and temp directories
.tmp_write_probe
.tmp_*
.tmp/

# Profiling artifacts
flamegraph.svg
perf.data
perf.data.old

# One-off analysis output
user_requests_output.txt
# Root-level ad-hoc logs are almost always local run artifacts. JSON config is
# intentionally not ignored wholesale; important project metadata often uses
# root-level JSON names and must stay visible to `git status`.
/*.log

# Refactor proof artifacts belong under docs/artifacts/refactor-runs/.
/refactor/

# Beads daemon rotated logs
.beads/daemon-*.log.gz

# Beads ephemeral files (locks, migration hints, backups)
.beads/.bv.lock
.beads/*.lock
.beads/.migration-hint-ts
.beads/*.bak_*

# Test log output
test-logs/

# Beads migration baseline artifacts
.beads/migration_baseline/

# Ephemeral files (agent-generated, build artifacts, test detritus)
ci-artifacts/
proptest-regressions/
test_meminfo
path_test

# One-off scan output
scan.txt

# Core dumps and agent config backups
/core.*
/core
/*.mcp.json
*.mcp.json.*.bak
cline.mcp.json
opencode.json
windsurf.mcp.json
clippy.txt
clippy.json
clippy_results.json
clippy_unwrap.json
clippy_unwrap_clean.txt
check_output.txt
.next_backup*/
storage.sqlite3
storage.sqlite3-shm
storage.sqlite3-wal
storage.sqlite3.bak
storage.sqlite3.corrupt-*
storage.sqlite3-shm.corrupt-*
storage.sqlite3-wal.corrupt-*

# Recovery build artifacts
.target_cass_recovery/

# /git-stash-janitor skill scratch tree (per-run mining workspace for accumulated
# stashes); regenerated on each invocation.
.stash_janitor_workspace/

# Root-level ad-hoc reproducer programs (e.g. test_dir_symlink.rs, test_xss.rs
# created to verify behavior outside the test harness). Real tests live under
# tests/ or in #[cfg(test)] modules; this glob applies only to the repo root.
/test_*.rs

# NTM per-repo state (rate-limit tracking, session metadata) — regenerated on demand
.ntm/
.claude/settings.local.json

# Golden-file actual-dump outputs — written by tests/golden_robot_json.rs and
# tests/pages_export_golden.rs when a golden diverges from expected, strictly
# for local diffing ahead of UPDATE_GOLDENS=1 regeneration. Never committed.
tests/golden/**/*.actual

# cargo-fuzz duplicate corpus tree, created when cargo-fuzz is invoked from
# inside the fuzz/ subcrate (default destination is `./fuzz/corpus/<target>/`,
# which from within fuzz/ resolves to `fuzz/fuzz/corpus/<target>/`). The real
# tracked corpus lives at fuzz/corpus/<target>/; this nested tree is a
# regeneratable mirror of the same seed files.
fuzz/fuzz/

# Beads compliance audit dir (managed by /beads-compliance-and-completion-verification skill)
/beads_compliance_audit/
